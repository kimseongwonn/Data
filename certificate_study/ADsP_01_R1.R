# 1.R 소개
# 1) 데이터 분석 도구의 현황
# 가. R의 탄생
#   - R은 오픈소스 프로그램으로 통계, 데이터 마이닝과 그래프를 위한 언어이다.
#   - 다양한 최신 통계분석과 마이닝 기능을 제공한다.
# 
# 나. 분석도구의 비교
# 
# 다. R의 특징
#   - 오픈 소스 프로그램으로 사용자 커뮤니티가 활발하고 많은 패키지가 수시로 업데이트 된다.
#   - 그래픽 및 성능이 상용 프로그램과 대등하거나 월등하다.
#   - 시스템 데이터 저장 방식으로 매번 데이터를 로딩할 필요없고 명령어 스토리도 저장 가능하다.
#   - 모든 운영체제 사용 가능하다.(윈도우, 맥, 리눅스)
#   - S 통계 언어를 기반으로 구현된 통계전문가들의 사실상의 표준 플랫폼이다.
#   - 객체 지향언어로 통계 결과값을 객체에 저장하여 필요한 부분을 호출하여 쉽게 활용 가능하다.
#   - 함수형 언어로 깔끔하고 단축된 코드로 매우 빠른 수행 속도와 디버깅 노력이 감소되며 병렬 프로그래밍으로 전환이 용이하다.
# 라. R Studio
#   - R을 사용하기 위한 오픈소스 프로그램
#   - 메모리 변수의 형태와 값을 볼 수 있고 스크립트 관리와 도큐먼테이션이 편리하다.
---------------------------------------------------------------------------------------------------------------------------------------
# 2. R 기초 -1
# 1) 통계 패키지
# 가. R studio 구성화면
#   - 스크립트 / 콘솔 / 워크스페이스 / 설치된 패키지 plot, help를 보여주는 창
# 나. R GUI의 화면 구성
#   *1 패키지(Package)
#     - 패키지 : R 함수와 데이터 및 컴파일된 코드의 모임
#   *2 프로그램 파일 실행
#     - source("파일명") : 스크립트로 프로그래밍된 파일 실행하기
#     - sink(file or append or split) : file(=파일명), append(파일에 결과 덮어쓰기 또는 추가), split(출력파일에만 출력하거나 콘솔창에 출력)
#   *3 배치모드(batch mode)
#     - 배치모드 : 사용자와 인터렉션이 필요하지 않은 방식 / 메일 돌아가야하는 시스템에서 프로세스를 자동화할 때 유용
# 2) 변수와 벡터 생성
#   - R 데이터 유형과 객체
#     - 숫자(Number) : integer, double
#     - 논리값(Logical) : TRUE, FALSE
#     - 문자(character) : "a", "abc"
----------------------------------------------------------------------------------------------------------------------------------------
# 2. R 기초 -2
# 1) 기초중에 기초
#   - 출력하기 : print(), cat()
# print("Hello, World!")
# 
#   - 변수에 값 할당하기 : <- , <<-, =, ->
# x <- 10
# print(x)
# y <- 3; z <- 5
# 
#   - 변수 목록보기 : ls(), ls str()
# ls() # 변수 이름만
# ls.str() # 변수 이름 + 객체들의 자세한 정보
# 
#   - 변수 삭제하기 : rm()
# rm(y, z)
# 
#   - 벡터 생성하기 : c()
# num_v <- c(1,2,3,4)
# char_v <- c("a", "b", "c", "d")
# logic_v <- c(TRUE, FALSE, TRUE)
# 
#   - 시퀀스 생성하기
# sequence <- 1:10
# 
#   - R 함수 정의하기 : function(a,b){실행되는 문장...}
# 
# my_func <- function(a, b) {
#   return (a+b)
# }
# 
# my_func(3,4)
# 
# 2) R 프로그램 소개
# - 데이터할당
# a <- 1
# 
# - 화면 프린트
# print(a)
# 
# - 벡터 생성 : c 함수는 벡터를 생성하는 함수로 숫자, 논리, 문자 모두 담을 수 있다.
# x1 <- c(1,2,3)
# x2 <- c('a','b','c')
# x3 <- c(TRUE, FALSE, TRUE)
# 
# - 수열
# seq1 <- 1:10
# print(seq1)
# 
# seq(시작점, 끝점, 간격) : 수열을 생성하는 함수
# seq2 <- seq(1,10,2)
# print(seq2)
# 
# # length.out = n >> 생성되는 수열의 길이가 n이 되도록 간격을 자동 조정한다.
# seq3 <- seq(1, 10, length.out = 5)
# print(seq3)
# 
# - 반복
# #rep(x, times = n) : x를 times 수 n 만큼 반복해
# rep1 <- rep(1, times = 5)
# # each = n : x가 벡터로 들어가고 벡터의 각 요소를 각각 2번씩 반복하여 새로운 벡터 생성
# rep2 <- rep(c(1,2,3), each = 2)
# # >> 1, 1, 2, 2, 3, 3
# 
# - 문자 붙이기
# # paste(붙일 문자열..., sep="구분자") :붙일 문자열들을 구분자로 붙여준다.
# # sep의 기본값은 공백
# 
# A <- paste("a", "b", "c", sep="-")
# print(A) # >> a-b-c
# 
# B <- paste(A, c("e", "f"), paste(A,10,sep=""))
# print(B)
# # paste(문자, 벡터(문자1, 문자2)) : "문자 문자1" "문자 문자2"로 구성된 벡터값으로 결과가 나온다.
# 
# - 문자열 추출
# substr(문자열, 시작점, 끝점) : 문자열에서 시작점에서 끝점 까지의 문자를 추출
# substr("abcdfegh", 3,5)
# 
# - 논리값
# # TRUE or FALSE
# 
# - 논리 연산자
# # ==, !=, <, <=, >, >=
# 
# - 벡터의 원소 선택하기
# vector <- c(1,2,3,4,5)
# print(vector[1]) # R의 인덱싱은 1부터 시작
# print(vector[2:4]) # 슬라이싱 범위 : 인덱스가 2이상 4이하
# 
# 3) 벡터의 연산
# - 연산자 우선 순위
# - [] : 인덱스
# - $ : 요소 뽑아내기
# - ^ : 지수
# - +, - : 단항의 양수, 음수 부호
# - : : 수열 생성 부호
# - %any% : 특수 연산자
# - *, / : 곱하기 나누기
# - +, - : 더하기 빼기
# - 비교 연산자
# - ! : 논리 부정
# - & : and
# - | : or
# - ~ : 식(formula)
# - ->,->> / = / <-, <-- : 대입 연산자
# - ? : 도움말
# 
# 4) 벡터의 기초 통계
# - mean() : 평균
# # 숫자 집합의 평균 계산
# nums <- c(1, 2, 3, 4, 5)
# average <- mean(nums)
# print(average)  # 출력: 3
# 
# - sum() : 합계
# # 숫자 집합의 합계 계산
# nums <- c(1, 2, 3, 4, 5)
# total <- sum(nums)
# print(total)  # 출력: 15
# 
# - median() : 중앙값
# # 숫자 집합의 중앙값 계산
# nums <- c(1, 2, 3, 4, 5)
# mid_val <- median(nums)
# print(mid_val)  # 출력: 3
# 
# - log() : 자연 로그값
# # 자연로그 계산
# num <- 10
# log_val <- log(num)
# print(log_val)  # 출력: 2.302585
# 
# - sd() : 표준편차
# # 숫자 집합의 표준편차 계산
# nums <- c(1, 2, 3, 4, 5)
# std_dev <- sd(nums)
# print(std_dev)  # 출력: 1.581139
# 
# - var() : 분산
# # 숫자 집합의 분산 계산
# nums <- c(1, 2, 3, 4, 5)
# variance <- var(nums)
# print(variance)  # 출력: 2.5
# 
# - cov() : 공분산
# # 두 벡터의 공분산 계산
# vec1 <- c(1, 2, 3, 4, 5)
# vec2 <- c(5, 4, 3, 2, 1)
# covariance <- cov(vec1, vec2)
# print(covariance)  # 출력: -2.5
# 
# 
# # 두 변수 간의 관계를 나타내는 통계량입니다.
# # 두 변수가 함께 어떻게 변하는지를 나타냅니다.
# # 공분산이 양수인 경우: 변수가 함께 증가하거나 감소하는 경향이 있음을 나타냅니다.
# # 공분산이 음수인 경우: 변수 중 하나가 증가할 때 다른 변수가 감소하는 경향이 있음을 나타냅니다.
# # 공분산이 0인 경우: 변수 간에 선형적인 관계가 없음을 나타냅니다.
# 
# - cor() : 상관계수
# # 두 벡터의 상관 계수 계산
# vec1 <- c(1, 2, 3, 4, 5)
# vec2 <- c(5, 4, 3, 2, 1)
# correlation <- cor(vec1, vec2)
# print(correlation)  # 출력: -1
# 
# # 두 변수 간의 선형적 관계의 강도와 방향을 나타내는 통계량입니다.
# # -1에서 1 사이의 값을 가집니다.
# # 상관계수가 1에 가까울수록 양의 선형 관계가 강하고, -1에 가까울수록 음의 선형 관계가 강합니다.
# # 상관계수가 0에 가까울수록 두 변수 간에 선형적 관계가 거의 없음을 나타냅니다.
# # 상관계수는 공분산을 각 변수의 표준편차로 나눈 값으로 계산됩니다.
# 
# - length() : 길이
# # 객체의 길이 계산
# vec <- c(1, 2, 3, 4, 5)
# len <- length(vec)
# print(len)  # 출력: 5
# 
# ---------------------------------------------------------------------------------------------------------------
# 3. 입력과 출력
# 1) 데이터 분석과정
# 2) R에서의 데이터 입력과 출력
# - 키보드로 데이터 입력
# # 데이터 양이 적을 때 직접 입력
# # C() 등등
# 
# - 출력할 내용의 자리수 정의
# # digits=num
# num1 = 10
# num2 = 3
# num3 = num1/num2
# print(num3, digits=4) # 출력물이 4자리로 출력되도록 설정
# 
# - 파일에 출력하기
# - 파일 목록 보기
# - Cannot Open File 해결하기
# - 고정자리수 데이터 파일 읽기
# - 테이블로 된 데이터 파일 읽기
# - CSV 데이터 파일 읽기
# - CSV 데이터 파일로 출력
# - 웹에서 데이터 파일 읽어 올 때
# - HTML에서 테이블 읽어 올 때
# - 복잡한 구조의 파일(웹 테이블) 읽기






















  